# Shared Python 3.10 stack for EMOCA + SMPLest-X inference
#
# This file is intentionally more concrete than the original modernization draft.
# It targets one environment that can run both projects with the same Torch/CUDA
# stack while avoiding the NumPy 2.x ABI breakage seen in EMOCA.

# Core numerical / vision stack
numpy==1.24.4
scipy==1.10.1
matplotlib==3.7.5
scikit-image==0.21.0
opencv-python==4.11.0.86
h5py==3.8.0

# PyTorch stack shared by both projects
#torch==2.0.1
#torchvision==0.15.2
#torchaudio==2.0.2

# EMOCA / GDL runtime
pytorch-lightning==1.9.5
torchmetrics==0.11.4
timm==1.0.14
omegaconf==2.3.0
hydra-core==1.3.2
fvcore
iopath
face-alignment==1.3.5
kornia==0.6.12
facenet-pytorch==2.5.2
#chumpy==0.70
mediapipe==0.10.14
protobuf>=4.25.3,<5
wandb==0.15.12
hickle==5.0.2
munch==2.5.0
adabound==0.0.5
compress_pickle==2.1.0
ffmpeg-python==0.2.0
imageio-ffmpeg==0.6.0
scikit-video==1.1.11

# SMPLest-X runtime
smplx==0.1.28
tqdm==4.67.1
trimesh==4.6.2
pyrender==0.1.45
pyopengl
json_tricks==3.17.3
einops==0.8.1
ultralytics==8.3.75
dill==0.3.8

# Build / tooling
pip-tools
setuptools<70.0.0

# PyTorch3D must match the selected torch/cuda build. Install it after the
# torch packages if pip cannot resolve a prebuilt wheel for your platform.
